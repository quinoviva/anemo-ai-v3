# Anemo AI — Local Training Requirements (Windows / CPU / GPU)
# ============================================================
# Flexible versions — tested on Windows 10/11, Python 3.9-3.12
#
# Install:
#   pip install -r scripts/ml/requirements_local.txt
#
# For GPU support (CUDA 12.x):
#   pip install tensorflow[and-cuda]
#   (replaces the cpu tensorflow below)

# ── Core Deep Learning ────────────────────────────────────────
tensorflow>=2.13.0,<2.16.0          # GPU: pip install tensorflow[and-cuda]
tensorflowjs>=4.10.0                # .h5 → TF.js converter

# ── Computer Vision ───────────────────────────────────────────
opencv-python-headless>=4.8.0       # headless (no GUI needed for training)
Pillow>=9.5.0
albumentations>=1.3.0

# ── Scientific Computing ──────────────────────────────────────
numpy>=1.24.0,<2.0.0               # numpy 2.x can break some TF builds
scipy>=1.10.0
scikit-learn>=1.3.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# ── Dataset Acquisition ───────────────────────────────────────
kaggle>=1.6.0
requests>=2.28.0
tqdm>=4.65.0
gdown>=4.7.0                        # Google Drive fallback

# ── Calibration & Evaluation ──────────────────────────────────
# netcal — optional, only needed for advanced calibration
# Uncomment if needed: netcal>=1.3.0

# ── Jupyter (local notebook support) ─────────────────────────
jupyter>=1.0.0
ipykernel>=6.20.0
ipywidgets>=8.0.0
